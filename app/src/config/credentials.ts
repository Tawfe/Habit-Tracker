/**
 * Supabase credentials for running the app.
 *
 * Paste your two values here to get going. The anon/publishable key is PUBLIC
 * by design — it ships inside the app binary and is protected by Row Level
 * Security — so it's safe to keep here. Do NOT put the service_role key here.
 *
 * Find both in: Supabase dashboard → Project Settings → API.
 *
 * (On Android these can alternatively come from .env via react-native-config;
 *  this file is the simple cross-platform path and is what iOS uses.)
 */
export const CREDENTIALS = {
  supabaseUrl: '',
  supabaseAnonKey: '',
};
